# plan_server.py dependencies (plan-server container)
fastapi==0.115.12
uvicorn==0.34.3

# plan_mcp.py dependencies (runs inside claude-server — these are
# already installed there via agent/claude/requirements.txt, but
# listed here for completeness and standalone testing)
mcp>=1.0.0
requests>=2.31.0

# Test dependencies
pytest==8.3.5
pytest-asyncio==0.24.0
httpx==0.28.1
